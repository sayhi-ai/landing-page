import React from "react";
import Signup from "./Signup";

var Layout = React.createClass({
    render() {
        return (
            <div>
                <Signup/>
            </div>
        )
    }
})

export default Layout